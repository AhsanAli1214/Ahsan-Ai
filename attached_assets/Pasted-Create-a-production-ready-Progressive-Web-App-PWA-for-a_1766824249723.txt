Create a production-ready Progressive Web App (PWA) for a privacy-first AI platform called "Ahsan AI Hub".

Requirements:
- Works on Android, iOS, Windows, macOS
- Installable (Add to Home Screen)
- Offline support using Service Worker
- Proper Web App Manifest
- PWA-compatible icons and splash screen
- Optimized for Next.js and Vercel
- SEO and performance friendly
- No paid services

Tech stack:
- Next.js (App Router)
- JavaScript
- Vercel deployment

Provide:
- manifest.json
- service worker
- Next.js configuration
- layout/head configuration
- install prompt handling
- offline fallback
- testing checklist
🧩 STEP-BY-STEP REAL SETUP (Next.js + Vercel)
1️⃣ Install PWA Dependency
bash
Copy code
npm install next-pwa
2️⃣ next.config.js
js
Copy code
const withPWA = require("next-pwa")({
  dest: "public",
  register: true,
  skipWaiting: true,
  disable: process.env.NODE_ENV === "development",
});

module.exports = withPWA({
  reactStrictMode: true,
});
3️⃣ Create public/manifest.json
json
Copy code
{
  "name": "Ahsan AI Hub",
  "short_name": "AI Hub",
  "description": "Privacy-first AI platform with free tools for chat, content creation, and more.",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0f172a",
  "theme_color": "#0f172a",
  "orientation": "portrait",
  "scope": "/",
  "icons": [
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
📁 Create folder:

swift
Copy code
public/icons/
Use icon sizes:

192x192

512x512

4️⃣ App Router Layout (app/layout.js)
js
Copy code
export const metadata = {
  title: "Ahsan AI Hub",
  description: "Privacy-first AI platform with free tools",
  manifest: "/manifest.json",
  themeColor: "#0f172a",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link rel="apple-touch-icon" href="/icons/icon-192.png" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
      </head>
      <body>{children}</body>
    </html>
  );
}
5️⃣ Offline Fallback Page
Create:

bash
Copy code
app/offline/page.js
js
Copy code
export default function OfflinePage() {
  return (
    <div style={{ padding: 40, textAlign: "center" }}>
      <h1>You're Offline</h1>
      <p>Please check your internet connection.</p>
    </div>
  );
}
6️⃣ Service Worker (Auto handled)
next-pwa automatically generates:

arduino
Copy code
public/sw.js
✔ No manual setup required
✔ Handles caching & offline mode

7️⃣ Add Install Button (Optional but Recommended)
Create:

Copy code
components/InstallPWA.js
js
Copy code
"use client";
import { useEffect, useState } from "react";

export default function InstallPWA() {
  const [prompt, setPrompt] = useState(null);

  useEffect(() => {
    const handler = (e) => {
      e.preventDefault();
      setPrompt(e);
    };
    window.addEventListener("beforeinstallprompt", handler);
    return () => window.removeEventListener("beforeinstallprompt", handler);
  }, []);

  if (!prompt) return null;

  return (
    <button
      onClick={() => {
        prompt.prompt();
        setPrompt(null);
      }}
    >
      Install App
    </button>
  );
}
Use it anywhere:

js
Copy code
<InstallPWA />
8️⃣ Vercel Settings (IMPORTANT)
✔ No special config needed
✔ Just deploy normally
✔ HTTPS auto-enabled (required for PWA)

🧪 TEST YOUR PWA
Chrome
nginx
Copy code
DevTools → Lighthouse → Progressive Web App
Mobile
Open site

Menu → Add to Home Screen

iOS (Safari)
Share → Add to Home Screen

🚀 FINAL RESULT
✅ Installable app
✅ Offline mode
✅ App-like UI
✅ Fast loading
✅ Google PWA compliant
✅ Works on ALL DEVICES